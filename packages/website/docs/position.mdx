---
id: position
title: Position 定位
---

import { Position } from 'ali-react-overlays';
import { PlacementDiagram } from 'website/src/examples/position.stories';

## 使用场景

容器内定位组件。`<Position />` 是一个较为底层的组件，一般不会单独进行使用。

Position 提供了 [对话框](dialog)、[吐司提示](toast) 等上层组件所需的**容器内部定位能力**。

<PlacementDiagram />

## API

<PropsTable component={Position} />
